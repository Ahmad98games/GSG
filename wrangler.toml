name = "noxishub"
compatibility_date = "2024-04-01"

[assets]
directory = "./omnora-prime-v13/out"
not_found_handling = "single-page-application"

# Build runs in GitHub Actions (or: npm run build from repo root).
# Do not use [build] here — it hides failures and duplicates CI.
